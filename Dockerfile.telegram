FROM node:20-alpine

WORKDIR /app

COPY package*.json ./
# Incluir devDependencies (tipos React, etc.) por si se ejecuta build
RUN npm install --include=dev

COPY . .

EXPOSE 3001

CMD ["npm", "run", "chatbot"]
